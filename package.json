{
  "name": "cypress-automation--training",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "triggerAllTest-headless": "npx cypress run",
    "triggerAllTest-headed": "npx cypress run --headed",
    "triggerAllTest-chrome": "npx cypress run --browser chrome",
    "triggerAllTest-dashboard": "npx cypress run --record --key [key]"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.6.4"
  },
  "devDependencies": {
    "cypress-multi-reporters": "^1.6.4",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
